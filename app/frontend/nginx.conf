server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router 클라이언트 라우팅 (SCR001~018) 지원 — 모든 경로를 index.html로
    location / {
        try_files $uri $uri/ /index.html;
    }
}
