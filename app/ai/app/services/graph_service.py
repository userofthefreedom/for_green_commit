"""Knowledge Graph(Neo4j) 질의 서비스. 실제 그래프 탐색은 Phase 4+에서 채운다."""


def find_recommendation_paths(user_id: str) -> list[dict]:
    # TODO(Phase 4+): Neo4j Cypher 질의로 User-Skill-Repo-Issue-Rule 경로 탐색.
    return []
